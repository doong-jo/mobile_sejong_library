<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backPink"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/barcode_bookImage"
        style="@style/Image.BookImage"
        android:layout_margin="@dimen/activity_vertical_margin"
        app:srcCompat="@drawable/bookimage_01" />

    <TextView
        android:id="@+id/barcode_bookTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textSize="@dimen/Barcode_BookTitle_TextSize"
        android:textColor="@color/black"
        android:text="책 제목" />

    <TextView
        android:id="@+id/barcode_subTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginVertical="@dimen/activity_vertical_margin"
        android:gravity="center"
        android:text="대출하시겠습니까?"
        android:textColor="@color/black"
        android:textSize="@dimen/Barcode_Title_TextSize"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="@dimen/activity_vertical_margin"
        android:layout_marginHorizontal="@dimen/Barcode_Horizontal_margin"
        android:orientation="horizontal">

        <Button
            android:id="@+id/barcode_borrowBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="네"
            android:textSize="@dimen/Barcode_Button_TextSize"
            android:textColor="@color/white"
            android:layout_weight="1"
            android:layout_marginHorizontal="@dimen/Barcode_Horizontal_margin"
            android:background="@color/btnPink"/>

        <Button
            android:id="@+id/barcode_cancelBtn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="아니요"
            android:textSize="@dimen/Barcode_Button_TextSize"
            android:textColor="@color/white"
            android:layout_weight="1"
            android:layout_marginHorizontal="@dimen/Barcode_Horizontal_margin"
            android:background="@color/btnPink"/>

    </LinearLayout>

    <TextView
        android:id="@+id/barcode_subInfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center|bottom"
        android:layout_marginTop="@dimen/Barcode_SubDate_marginTop"
        android:text="학술정보원모바일"
        android:textSize="@dimen/Barcode_SubDate_TextSize"/>

</LinearLayout>